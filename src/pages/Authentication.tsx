import { Container, Center } from '@mantine/core'; // Importing Center from Mantine

const Authentication = () => {
  return (
    <Center
      style={{
        height: '100vh', // Full viewport height
        background: '#3A3A3C', // Green to yellow gradient
      }}
    >
      <Container size="xs">
       hi
      </Container>
    </Center>
  );
};

export default Authentication;
